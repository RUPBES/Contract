@using MvcLayer.Models;
@using Newtonsoft.Json;
@model List<MvcLayer.Models.ServiceGCViewModel>

@{
    int index = 0;
    var d = new List<ServiceGCViewModel>();

    if (TempData["serviceGC"] is string s)
    {
        d.AddRange(JsonConvert.DeserializeObject<List<ServiceGCViewModel>>(s));
    }

    bool isFact = d.FirstOrDefault().IsFact == true ? true : false;

    string actionName = isFact ? "EditService" : "Create";
}
<style>
    .cont-table {
        overflow-x: scroll;
    }

    th,
    td {
        min-width: 250px;
    }
</style>
<h4>Услуги генподрядчика</h4>
<hr />
<form asp-action=@actionName class="row">
    <div class="cont-table">
        <table class="table table-bordered table-striped">
            <tr>

                <th>Период</th>
                @foreach (var item in d)
                {
                    <td>
                        @item.Period?.ToString("MMM yyyy")
                        <input asp-for="@Model[index].Period" type="text" class="form-control" value="@item.Period" hidden />
                    </td>
                    index++;
                }
                @{
                    index = 0;
                }
            </tr>
            @if (!isFact)
            {
                <tr>
                    <th>Процент генуслуг</th>
                    @foreach (var item in d)
                    {
                        <td>
                            @item.ServicePercent
                            <input asp-for="@Model[index].ServicePercent" class="form-control" type="number" step="0.01" min="0" max="100" value="@item.ServicePercent" />
                        </td>
                        index++;
                    }
                    @{
                        index = 0;
                    }
                </tr>
                <tr>
                    <th>Сумма генуслуг</th>
                    @foreach (var item in d)
                    {
                        <td><input asp-for="@Model[index].Price" class="form-control" type="number" step="0.01" min="0" /></td>
                        index++;
                    }
                    @{
                        index = 0;
                    }
                </tr>
            }
            @if (isFact)
            {
                <tr>
                    <th>Сумма генуслуг фактическая</th>
                    @foreach (var item in d)
                    {
                        <td><input asp-for="@Model[index].FactPrice" class="form-control" type="number" step="0.01" min="0" /></td>
                        index++;
                    }
                    @{
                        index = 0;
                    }
                </tr>
            }

            @foreach (var item in d)
            {
                @if (isFact)
                {
                    <input asp-for="@Model[index].Id" hidden />
                    <input asp-for="@Model[index].ServicePercent" hidden />
                    <input asp-for="@Model[index].Price" hidden />
                }

                <input asp-for="@Model[index].AmendmentId" hidden />
                <input asp-for="@Model[index].ContractId" hidden />
                <input asp-for="@Model[index].IsChange" hidden />
                <input asp-for="@Model[index].ChangeServiceId" hidden />

                index++;
            }

        </table>
    </div>
    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>

<div>
    <a asp-action="Index">Вернуться назад</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
