@model IEnumerable<MvcLayer.Models.PaymentViewModel>
@using BusinessLayer.Helpers;
@inject BusinessLayer.Interfaces.ContractInterfaces.IContractService Contract
@inject BusinessLayer.Interfaces.ContractInterfaces.IAmendmentService Amendment
@inject BusinessLayer.Interfaces.ContractInterfaces.ISWCostService sw
@inject BusinessLayer.Interfaces.ContractInterfaces.IScopeWorkService Scope
@inject BusinessLayer.Interfaces.ContractInterfaces.IFormService Form

@{
    ViewData["Title"] = string.Empty;
    int backId = 0;
    if (ViewBag.returnContractId == 0)
        backId = ViewBag.contractId;
    else backId = ViewBag.returnContractId;
    int? id = ViewBag.contractId;
    var contract = Contract.GetById((int)id);
    decimal? form = 0;
    decimal? scopeWork = 0;
    decimal? stillMust = 0;
    decimal? contractPrice = 0;
    if (id is not null)
    {
        contractPrice = Amendment.Find(x => x.ContractId == contract.Id).Select(x => x.ContractPrice).LastOrDefault();
        if (contractPrice == null || contractPrice == 0) contractPrice = contract.ContractPrice;
        var sworkId = Scope?.Find(x => x.ContractId == id)?.LastOrDefault()?.Id;
        scopeWork = sw?.Find(x => x.ScopeWorkId == sworkId && x.Period.Value.Year == DateTime.Today.Year).Sum(x => x.CostNds);
        form = contract?.FormC3as?.Where(x => x.ContractId == id).Sum(x => x.TotalCost);
        stillMust = contractPrice - form;
        ViewData["Title"] = $"Денежные средства по договору № {contract?.Number}";
    }
}
<style>
    .dm-modal {
        background-color: lightsteelblue;
    }

    .close-btn {
        color: red
    }

        .close-btn::after {
            border: 2px solid #F00 !important;
        }

        .close-btn:hover::after {
            border-color: red !important;
            color: red !important;
            -webkit-transform: scale(1);
            -moz-transform: scale(1);
            -ms-transform: scale(1);
            transform: scale(1);
            font-weight: bold;
        }
</style>
<div class="dm-overlay">
    <div class="dm-table">
        <div class="dm-cell">
            <div class="dm-modal">
                <div class="top-modal-block">
                    <label class="my-modal-title">@ViewData["Title"]</label>
                    @if (backId != null && backId > 0)
                    {
                        <a asp-controller="Contracts" asp-action="Details" asp-route-id="@backId" class="close-btn"></a>
                    }
                    @if (backId == null || backId == 0)
                    {
                        <a asp-controller="Contracts" asp-action="" class="close-btn"></a>
                    }
                </div>
                <hr class="modal-line" />
                <div class="modal-overflow-70">
                    <table class="table table-bordered table-main table-hover">
                        <thead>
                            <tr>
                                <th rowspan="2"><span>Контрактная цена, с НДС</span></th>
                                <th rowspan="2"><span>Остаток по выполнению</span></th>
                                <th rowspan="2"><span>Объем на текущий год</span></th>
                                <th rowspan="2"><span>Выполнено с-3а</span></th>
                                @{
                                    foreach (var item in Model)
                                    {
                                        <th colspan="2"><span class="span-month-double text-center">@item?.Period?.ToString("MMMM-yyyy")</span></th>
                                    }
                                }
                            </tr>
                            <tr>
                                @{
                                    for (int i = 0; i < Model.Count(); i++)
                                    {
                                        <th><span class="span-50-per">к оплате </span></th>
                                        <th><span class="span-50-per">на счет РУП "БЭС" - УКХ"</span></th>
                                    }
                                }
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                @if (Model.Count() > 0)
                                {
                                    <td class="text-end"><span>@contractPrice?.ToString("N2")</span></td>
                                    <td class="text-end"><span>@stillMust?.ToString("N2")</span></td>
                                    <td class="text-end"><span>@scopeWork?.ToString("N2")</span></td>
                                    <td class="text-end"><span>@form?.ToString("N2")</span></td>
                                    @foreach (var item in Model)
                                    {
                                        <td class="text-end"><span>@item.PaySum?.ToString("N2")</span></td>
                                        <td class="text-end"><span>@item.PaySumForRupBes?.ToString("N2")</span></td>
                                    }
                                }
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>